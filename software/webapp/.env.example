# Copy to .env and enter the values for your own deployment.
# Use plain KEY=value lines; quotes are optional. No variable expansion.
DB_HOST=localhost
DB_NAME=pv_monitoring
DB_USER=pv_monitoring
DB_PASSWORD=
# Use the same random hexadecimal key in the firmware secrets.h file.
DEVICE_API_KEY=
